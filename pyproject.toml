[project]
name = "energy-meter"
version = "0.1.0"
description = "Energy meter Modbus reader."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pymodbus<=3.8.3",
    "pytest>=8.3.4",
]
[tool.black]
line-length = 80
